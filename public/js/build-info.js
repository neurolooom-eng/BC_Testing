// Placeholder committed to git for local/dev use. The GitHub Actions
// deploy workflow (.github/workflows/deploy-pages.yml) OVERWRITES this file
// on every deploy with the real version (from VERSION), build number
// (github.run_number — numeric, monotonically increasing across all
// branches), and build date/time (UTC, at deploy time). Do not hand-edit
// buildNumber/buildDate; do bump VERSION per VERSIONING.md when relevant.
window.BUILD_INFO = { version: "1.0.0", buildNumber: 0, buildDate: "unbuilt (local)" };
